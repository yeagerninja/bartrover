twist_mux:
  ros__parameters:
    use_stamped: false
    topics:
      navigation:
        topic   : cmd_vel
        #topic   : cmd_vel_nav
        timeout : 0.5
        priority: 20
      tracker:
        topic   : cmd_vel_tracker
        timeout : 0.5
        priority: 40
      joystick:
        topic   : cmd_vel_joy
        timeout : 0.5
        priority: 70
      keyboard:
        topic   : cmd_vel_keyboard
        timeout : 0.5
        priority: 120